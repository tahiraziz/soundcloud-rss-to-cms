# Base URL of the deployed CORS proxy worker (see worker/). Get this from
# `npm run deploy` output in worker/, then copy this file to .env.local and
# fill it in.
VITE_RSS_PROXY_URL=https://soundcloud-rss-to-cms-proxy.<your-subdomain>.workers.dev
